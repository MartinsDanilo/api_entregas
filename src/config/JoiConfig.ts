// import Joi from 'joi';

// Joi.objectId = () => {
//     return Joi.string().regex(/^[0-9a-fA-F]{24}$/);
// };
// Joi.jwt = () => {
//     return Joi.string().regex(
//         /^[A-Za-z0-9-_]+\.[A-Za-z0-9-_]+\.[A-Za-z0-9-_.+/=]*$/,
//     );
// };
// Joi.timeSpan = () => {
//     return Joi.string().regex(/^\d{2}:\d{2}$/gi);
// };
